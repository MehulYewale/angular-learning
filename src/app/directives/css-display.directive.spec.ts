import { CssDisplayDirective } from './css-display.directive';

describe('CssDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new CssDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
